import { storyblokEditable } from "@storyblok/react";
import { render } from "storyblok-rich-text-react-renderer";

const About = ({ blok }) => {
  console.log(blok);
  return <div className="pl-8 flex items-center h-screen gap-6 ">hey</div>;
};

export default About;
